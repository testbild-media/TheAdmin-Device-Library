# TheAdmin Device Library

Default device definitions for TheAdmin and TheAdmin Device Manager.

## Publishing a release

Create and publish a GitHub release with a semantic version tag such as
`v1.0.0`. GitHub Actions validates all devices and attaches a clean `.adlib`
archive to the release. The archive contains only `library.json` and the
`device-assets` directory.
